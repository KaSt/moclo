version: v1.0
name: Test
agent:
  machine:
    type: f1-standard-2
    os_image: ubuntu2204
blocks:
  - name: Test
    task:
      jobs:
        - name: Test
          commands:
            - checkout
            - sudo apt update
            - sudo apt install -y build-essential git
            - git submodule update --init --recursive
            - make test
